# Что именно считается покрытием — и почему это стоит записать.
#
# Без этого файла Codecov берёт всё, что приехало в отчёте, и считает процент
# по нему: генерируемые файлы, конфиги и сборочные артефакты тянут цифру в
# любую сторону, ничего не говоря о качестве тестов. Пороги ниже — тоже
# решение: статус, падающий от шума в десятые доли, через неделю начинают
# игнорировать.

coverage:
  status:
    project:
      default:
        threshold: 1%
    patch:
      default:
        # С новым кодом строже, чем со старым: догонять покрытие задним числом
        # никто не станет, а не дать ему просесть — дёшево.
        target: 70%
        threshold: 5%

# Агент и бот — два независимых бинаря на Go, и «упало покрытие» должно
# указывать на конкретный. Страница на Astro покрытия не даёт вовсе: юнит-тестов
# у неё нет, её проверяют сквозные тесты, а они покрытие не считают. Здесь это
# записано явно, чтобы процент не читали как «покрытие всего репозитория».
flags:
  agent:
    paths:
      - agent/
    carryforward: true
  bot:
    paths:
      - bot/
    carryforward: true

ignore:
  - "src/**"
  - "e2e/**"
  - "scripts/**"
  - "docs/**"
  - "public/**"
  - "config/**"
  - "data/**"

comment:
  layout: "reach, diff, flags"
  require_changes: true
