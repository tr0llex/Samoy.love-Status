dist/
.astro/
node_modules/
stage/
package-lock.json

# Данные пишет пробер машинально — форматировать их незачем.
data/

# Вёрстка и токены форматируются вручную: правила в одну строку читаются
# как таблица значений, а Prettier раскладывает каждое свойство на строку и
# растит файл втрое, ничего не проверяя. Корректность .astro стережёт ESLint.
*.astro
*.css
