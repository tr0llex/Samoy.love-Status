# Цель: телеграм-бот статуса.

APP=samoylove-bot
ROOT=/opt/samoylove-bot
ARTIFACT_DIR=stage
UNIT=samoylove-bot.service
OWNER=root:samoylove-bot
GOARCH=arm64

# Кавычки ОДИНАРНЫЕ: подстановка должна случиться при сборке, а не при чтении
# этого файла — иначе переменные окажутся пустыми.
BUILD_CMD='rm -rf stage && mkdir -p stage && cd bot && GOOS=linux GOARCH=${DEPLOY_GOARCH:-arm64} CGO_ENABLED=0 go build -trimpath -ldflags "-s -w" -o ../stage/samoylove-bot . && cd ..'

# HEALTH нет и не будет: бот никуда не слушает — он сам ходит в Telegram
# длинным опросом. Что выкатка удалась, видно по ответу на /status; проверить
# это HTTP-запросом с сервера невозможно.

# Соседи проверяются после переключения: бот делит хост со статус-страницей,
# и выкатка не должна её задеть.
NEIGHBOURS=status.samoy.love

KEEP=5
WRITE_VERSION_FILE=0

# NOTIFY=all (умолчание) осознанно: статус-страница не следит сама за собой —
# ни её, ни агента, ни бота нет в config/status.json. Заглушить успех здесь
# значит остаться вообще без новостей об их выкатке.
